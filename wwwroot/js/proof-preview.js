document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('[data-proof-input]').forEach(input=>{const box=document.querySelector(input.dataset.previewTarget);if(!box)return;const img=box.querySelector('img');const remove=box.querySelector('[data-remove-proof]');const render=()=>{const file=input.files&&input.files[0];if(!file){box.classList.remove('is-visible');img.removeAttribute('src');return}if(!file.type.startsWith('image/'))return;const reader=new FileReader();reader.onload=e=>{img.src=e.target.result;box.classList.add('is-visible')};reader.readAsDataURL(file)};input.addEventListener('change',render);remove?.addEventListener('click',()=>{input.value='';render()})})});